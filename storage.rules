rules_version = '2';

// TEST MODE: open access for development only.
// Replace with production role-based rules before launch.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
